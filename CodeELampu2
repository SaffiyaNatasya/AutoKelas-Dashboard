const options = {
  username: "sfftsya",
  password: "Tasya_0507",
  clean: true,
  connectTimeout: 4000
};

const client = mqtt.connect(
  "wss://178478c035b34dcfb27a2f6c1bf18591.s1.eu.hivemq.cloud:8884/mqtt",
  options
);

client.on("connect", () => {
  console.log("Connected to HiveMQ");
});

client.on("error", (err) => {
  console.error("MQTT error:", err);
});

function sendCmd(topic, message) {
  client.publish(topic, message);
  console.log(`Sent ${message} to ${topic}`);
}
